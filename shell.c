#include<stdio.h>
#include<stdlib.h>

char * get_input() {
	char line[256];
	fgets(line, 256, stdin);
	sscanf(input,"%s\n", line);
	char * formatted = calloc(256, sizeof(char));
	strncpy(formatted, )

}

char ** parse_args{

}
